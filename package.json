{
  "name": "com.stripe.billing.extensions",
  "version": "0.0.1",
  "description": "Stripe Billing Extensions",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "pnpm lint:types && pnpm lint:eslint && pnpm lint:format",
    "lint:types": "pnpm -r --if-present lint:types",
    "lint:eslint": "eslint . --ignore-pattern 'extensions/**' && pnpm -r --filter './extensions/*' --if-present lint:eslint",
    "lint:format": "prettier --check .",
    "fix:lint": "eslint --fix . --ignore-pattern 'extensions/**' && pnpm -r --filter './extensions/*' --if-present fix:lint",
    "fix:format": "prettier --write .",
    "test": "node tools/test.js",
    "test:watch": "vitest watch",
    "check": "pnpm build && pnpm lint && pnpm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@stripe/extensibility-dev-tools": "^0.21.0",
    "@stripe/extensibility-eslint-plugin": "^0.14.0",
    "@types/node": "^20.19.0",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-workspaces": "^0.12.1",
    "globals": "^17.4.0",
    "jiti": "^2.6.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "vitest": "^3.0.0",
    "@stripe/ui-extension-sdk": "^9.1.0"
  }
}
