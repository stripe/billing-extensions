{
  "name": "maximum_credit_amount_per_invoice",
  "version": "1.0.2",
  "description": "maximum_credit_amount_per_invoice",
  "private": true,
  "license": "~~proprietary~~",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && pnpm build:schemas",
    "build:schemas": "gen-schemas --root ../.. --extension-id maximum_credit_amount_per_invoice --out-dir generated --schema-name config",
    "lint": "pnpm lint:types && pnpm lint:eslint && pnpm lint:format",
    "lint:types": "tsc -p tsconfig.build.json --noEmit && tsc -p tsconfig.json --noEmit",
    "lint:eslint": "eslint .",
    "lint:format": "prettier --check .",
    "fix:lint": "eslint --fix .",
    "fix:format": "prettier --write .",
    "test": "vitest --root ../.. run --project \"$npm_package_name\"",
    "test:watch": "pnpm test --watch",
    "dev": "concurrently -n build,lint,test -c blue,yellow,green 'tsc -p tsconfig.json -w --noEmit' 'chokidar \"src/**/*.{ts,json}\" -c \"eslint .\"' 'pnpm test --watch --no-clear-screen'",
    "stripe:regen": "gen-workspace --root ../.. --package-dir . --template-id billing.customer_balance_application"
  },
  "lint-staged": {
    "*.{ts,mts,mjs,js}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yaml}": "prettier --write"
  },
  "dependencies": {
    "@stripe/extensibility-sdk": "1.0.1"
  }
}
